# Project Customization Rules

- **Git Push Constraint**: Only stage and commit changes locally. Do NOT push commits to the remote origin on GitHub (e.g. `git push`) unless the user explicitly asks to push.
